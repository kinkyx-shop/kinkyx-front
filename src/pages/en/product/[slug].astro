---
import ProductView from "@/components/ProductView.astro";
import { productStaticPaths } from "@/lib/routes";

export function getStaticPaths() {
  return productStaticPaths("en");
}

const { productKey } = Astro.props as { productKey: number };
---

<ProductView locale="en" productKey={productKey} />
