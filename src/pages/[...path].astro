---
import CategoryView from "@/components/CategoryView.astro";
import { categoryStaticPaths } from "@/lib/routes";

export function getStaticPaths() {
  return categoryStaticPaths("fr");
}

const { categoryKey } = Astro.props as { categoryKey: number };
---

<CategoryView locale="fr" categoryKey={categoryKey} />
